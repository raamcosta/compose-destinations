package com.ramcosta.composedestinations.codegen.model

/**
 * The different available core modules for the library.
 *
 * [MAIN] is the basic module with most of the implementations whereas [ANIMATIONS] is the module
 * containing the animation implementations (Based on the Accompanist library).
 */
enum class Core {
    MAIN, ANIMATIONS
}